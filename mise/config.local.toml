# Per-machine mise overrides. Copied once by install.sh; safe to edit freely.
# Shared baseline lives in ~/.config/mise/conf.d/fresh.toml (symlinked from the dotfiles repo).
# Local entry point is ~/.config/mise/config.toml (copied from the repo, also editable).
#
# Example:
# [tools]
# node = "22"
# python = "3.13"

[tools]
